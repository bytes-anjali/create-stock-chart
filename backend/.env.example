# Copy to .env and fill in values
# Place your OAuth client_secret.json in backend/ (download from Google Cloud Console)

# Must match an Authorized Redirect URI in your Google Cloud OAuth credentials
REDIRECT_URI=http://localhost:8000/api/auth/callback

# Frontend origin for CORS
FRONTEND_ORIGIN=http://localhost:5173
